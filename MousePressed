function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
	if(mouseIsPressed && mouseX >160 && mouseX <260 && mouseY >160 && mouseY <260 ){
		background(0,250,0)
		rect(160,160,100,100);
	}
	else{
		background(255)
		rect(160,160,100,100);
	}
